# =============================================================================
# AI PR Review — Frontend Environment Variables
# =============================================================================
# API base URL (without trailing slash)
# In production, this would be the deployed backend URL
NEXT_PUBLIC_API_BASE=http://localhost:8000/api/v1
